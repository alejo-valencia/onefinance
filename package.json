{
  "scripts": {
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json}\"",
    "deploy": "cd functions && npm run deploy && cd ../web && npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "react-router-dom": "^7.12.0"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
